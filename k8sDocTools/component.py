#!/usr/bin/python3

from globals import core_list
from __init__ import __version__

Class charm():
    def __init__(self,name,version):
      self.name=name
      self.discover(False)
    def __init__(self,name,revision):
      self.name= name
      self.revision=str(revision)
    def discover(self,revision)
      if not revision:
         # get latest revision from store
      # get stuff
      # generate page


def charm_generate(bundle):
    pass
